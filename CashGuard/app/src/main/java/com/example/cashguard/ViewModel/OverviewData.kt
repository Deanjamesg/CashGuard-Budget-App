package com.example.cashguard.ViewModel

// Simple data class to hold the calculated overview values
data class OverviewData(
    val totalIncome: Double = 0.0,
    val totalExpenses: Double = 0.0,
    val savings: Double = 0.0
)
